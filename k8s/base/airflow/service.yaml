apiVersion: v1
kind: Service
metadata:
  name: airflow
  labels:
    app.kubernetes.io/name: airflow
spec:
  selector:
    app.kubernetes.io/name: airflow
  ports:
    - name: http
      port: 8080
      targetPort: 8080
