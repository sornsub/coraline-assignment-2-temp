apiVersion: v1
kind: Service
metadata:
  name: orion-api
  labels:
    app.kubernetes.io/name: orion-api
spec:
  selector:
    app.kubernetes.io/name: orion-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
